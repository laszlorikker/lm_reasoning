# Exact pins that passed scripts/check_env.py on this box (2026-08-31, WSL2,
# Quadro RTX 5000 Max-Q, driver CUDA 12.4). See CLAUDE.md for the gate list.
#
# Install (torch comes from the cu126 index; runs on the 12.4 driver via CUDA
# minor-version compatibility — verified by the gates):
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu126
torch==2.10.0+cu126
transformers==5.16.1
peft==0.20.0
bitsandbytes==0.50.2
datasets==5.0.1
accelerate==1.14.0
numpy==2.5.2
PyYAML==6.0.3
pytest==9.1.1
tensorboard==2.21.0
# M1 additions
pysbd==0.3.4
spacy==3.8.16
sacrebleu==2.6.0
